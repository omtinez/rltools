.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install rltools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rltools
    $ pip install rltools
